---
title: Choices Webapp
aliases: [choices-webapp, choices]
tags: [project, webapp, aws, serverless, game]
type: project
status: evergreen
created: 2026-07-01
updated: 2026-07-21
related: [[Projects MOC]], [[Choices Growth Plan]], [[Choices Suggestion Engine Plan]], [[Choices Data Architecture Plan]]
---
# Choices Webapp
Lightweight two-player, turn-based elimination game. Player A creates a game, pre-seeds 4 choices, and shares a human-friendly code (e.g. `PLUM-42`); Player B joins and they alternate eliminating choices (B → A → B) until one winner remains. No accounts — flows through messaging apps.

## Goal
Fully serverless, shareable social game on AWS free tier. Turn alerts via Web Push, with polling fallback so it works everywhere.

## Stack
- **Frontend:** React 18 + Vite SPA; hash routing (`#/create`, `#/join?code=`); PWA (service worker + manifest) for iOS push. Hosted on private S3 + CloudFront (HTTPS). `src/` is feature-foldered (2026-07-20): `features/{game,account,premium,admin,places}` + shared `components/` (primitives + chrome) + `hooks/` + `lib/` (api/auth/push/platform/storage/streakCache); `@/` Vite alias → `src/` (jsconfig.json for IntelliSense); every routed screen lives in a feature, single global `styles.css` stays at root.
- **Backend:** Node 22 Lambda via **Function URLs** (no API Gateway); one handler routes 6 actions (createPairing, claimSeat, getState, eliminate, rematch, subscribe).
- **Data:** DynamoDB single table `choices-games` (on-demand, 30-day TTL auto-cleanup).
- **Notifications:** Web Push (VAPID) sent from Lambda; 3s polling fallback.
- **IaC:** AWS SAM (`template.yaml` + `samconfig.toml`).

## Key context
- **Game logic** is pure and I/O-free in `backend/game.mjs` (unit-tested via `game.test.mjs`, Node `--test`).
- **Identity/auth:** no login — role (A/B) scoped to pairing + URL, guarded by a `localStorage` token; all mutations validate `{ pairingId, role, token }`.
- **Data model:** `PAIR#{id}` (identity+state), `CODE#{code}` → pairing, games nested in pairing, `SUB#{pairingId}#{role}` (push subs). Pairing + subs survive rematches.

## Gotchas
- iOS Web Push only works when the app is added to Home Screen (PWA install, iOS 16.4+); game still fully playable via polling without it.
- HTTPS mandatory for Web Push → CloudFront required (raw S3 HTTP won't do).
- Push is fire-and-forget (404/410 = expired sub, logged and skipped).
- Deploy CORS permissive first, then tighten to the CloudFront origin.

## Deploy
- Backend: `sam build && sam deploy --guided` (Lambda + DynamoDB + S3 + CloudFront + CloudFormation).
- Frontend: `npm run build` → `aws s3 sync dist/ …` → `aws cloudfront create-invalidation`.
- VAPID keys generated locally (`npx web-push generate-vapid-keys`), passed as SAM params.
- Tests: `cd backend && npm test`.

## Status
Deployed at `choices.austinjuliuskim.com` (custom domain + ACM cert); v1 complete (last substantive commit Jun 7, 2026). Now executing a growth + monetization push — see [[Choices Growth Plan]].

Affiliate deep links + iOS-native UI + preview-stack deploys merged 2026-07-04 (PR #1). **Tier-1 cost hardening deployed to prod 2026-07-04**: API behind CloudFront at `https://choices.austinjuliuskim.com/api` (same-origin, no CORS preflights), adaptive polling (3s hot / 15s waiting / 30s idle / paused hidden), idempotent mutations (actionId replay + optimistic locking), billing alarms (BillingAlarms stack). WAF rate limiting lives in the CloudFront pricing-plan protection pack (Count mode, out of git — see [[Choices Growth Plan]] §10b for the plan constraints that shaped this). Still pending: origin-header enforcement (~day after cutover) and WAF Count→Block after soak.

**iOS Phase A in progress** (2026-07-04, branch `feature/ios-capacitor`): Capacitor 8 wrap on Apple's free tier (simulator + personal-team device installs; no paid enrollment until launch). Native haptics/share/browser polish; turn updates via the existing adaptive polling (no push entitlement on free tier). Two-phase ladder in [[Choices Growth Plan]] §3.

**Accounts + billing live 2026-07-05**: optional Cognito Google sign-in (hosted UI `choices-auth.auth.us-west-2.amazoncognito.com`, PKCE) and Stripe premium checkout ($2.99/mo, $24/yr) live on prod; preview stack mirrors with test-mode Stripe and `choices-auth-preview`. Webhook at `/api/stripe-webhook` (400 on unsigned posts — verified). Gotcha recorded: `deploy-frontend.sh` bakes `VITE_TIP_*` and Cognito env at build time — a local run without the GitHub repo variables ships a build with the tip jar hidden (CI injects them; export them for local runs).

**Suggestion engine (Phases 0/1/3) + growth features built 2026-07-07** — five branches pushed, PRs pending (merge order: suggest-phase0 → suggest-typeahead → fill-my-4; og-previews and reveal-copy independent). Per [[Choices Suggestion Engine Plan]]:
- `feature/suggest-phase0`: `HIST#{pairingId}` pair-memory item (capped 200 LRU, TTL tied to pairing lifecycle) folded into the completion transaction via new pure `backend/history.mjs`; anonymized per-game JSON to new private `SuggestDataBucket` (`entries/dt=…/`, HMAC pairHash, no pairing ids — `AnonSalt` SAM param gates it). `userPut` renamed `versionedPut`.
- `feature/suggest-typeahead`: `ChoiceInput.jsx` typeahead on create + rematch inputs — L1 pair memory (new `getPairHistory` action, fetched once, never on the poll path; ranking in pure `frontend/src/suggest.js`) + L3 Google Places Autocomplete (New) proxied server-side (`backend/places.mjs`, session-tokened, Essentials-tier details terminates billing session). Dormant without `PlacesApiKey` (stack output → `VITE_PLACES_ENABLED`).
- `feature/fill-my-4`: ✨ Fill my 4 via Bedrock Converse (`backend/suggestai.mjs`, Claude Haiku 4.5 profile via `BedrockModelId` param). 3 free/month — counter on the pairing (rematch) or USER# `aiUses` (create screen, sign-in required — documented deviation); premium unlimited; actionId replay never re-invokes Bedrock.
- `feature/og-previews`: share links now `/j/{CODE}` — new CloudFront behavior → Lambda prerenders OG meta (choices as description, `og-card.png` 1200×630 checked in) + instant redirect into the join flow; `obscenity`-based `backend/moderation.mjs` downgrades profane labels to a generic description at render time only (submission never blocked); server-side 60-char cap + control-char strip in `createGame`.
- `feature/reveal-copy`: `frontend/src/revealCard.js` canvas share card + 📸 button on the winner face (native share via new `@capacitor/filesystem`, web share, download fallback; `share-reveal` support-platform beacon) + Growth §9 copy pass (landing/invite/join/banners/pushes/title/manifest → "Decide what to eat, together").
New actions: `getPairHistory | placesSuggest | placeDetails | fillMyFour` and `GET /j/{code}`. All features config-dormant (AnonSalt / PlacesApiKey / BedrockModelId blank = off); ops steps live in each PR description.

**✨ Fill my 4 live on prod + preview 2026-07-09** (PR #24, branch `ops/enable-fill-my-4`): AWS Bedrock Claude Haiku 4.5 model access approved, so the config-dormant feature was switched on by setting `BedrockModelId=us.anthropic.claude-haiku-4-5-20251001-v1:0` (US cross-region inference profile, us-west-2) in both `samconfig.toml` deploy blocks. **No source changed** — the feature shipped complete in PR #11 (`feature/fill-my-4`). That one param cascades: Lambda `BEDROCK_MODEL_ID` env → `aiEnabled()` true → `fillMyFour` action; `AiEnabled` stack output (`= HasBedrock`) → `deploy-frontend.sh` → `VITE_AI_ENABLED=true`, which un-hides the button. IAM for `bedrock:InvokeModel` was already in the template. Verified live end-to-end (`createPairing → claimSeat → fillMyFour` → 200 with 4 Haiku choices, `usesLeft:2`) on both stacks. Rollback = blank `BedrockModelId` + redeploy backend/frontend (feature goes dormant instantly, fully reversible).

**Owner-only activity dashboard + EMF observability built 2026-07-09** (PR #26, branch `feature/admin-activity-dashboard`) — partially lands [[Choices Growth Plan]] §10. `backend/metrics.mjs` emits CloudWatch EMF (namespace `ChoicesApp`, hand-rolled, no dep): `GameCreated`/`GameCompleted`/`SeatClaimed`/`FillMyFour` counters, per-action `Latency` (p50/p95/p99), `ApiError`. New owner-only `getAdminOverview` action (gated by `assertAdmin` / `ADMIN_SUBS` Cognito-sub allowlist) does a projected `Scan` of `PAIR#` items → pure `aggregateActive` reducer (`backend/admin.mjs`) returning **anonymous aggregates only** — games in progress, whose-turn split, distinct active users, top choices behind a k-anon floor; **never a record** (constitution rules 6 & 9). Frontend `AdminView.jsx` at `#/admin` (owner-gated, above the identity gate), polls every 30s. The CloudWatch dashboard lives in a **separate admin-deploy stack** `ops/ops-dashboard.yaml` — the CI deploy role lacks `cloudwatch:PutDashboard` (same reason as `ops/billing-alarms.yaml`; putting it in the app template rolled the whole update back). Deployed to preview + prod. **Prod enabled 2026-07-09** — owner sub set on the prod stack via a CLI `--parameter-overrides AdminSubs=…` (deliberately NOT committed; persists across future deploys via CloudFormation UsePreviousValue, same as the Stripe/VAPID secrets), so `#/admin` is live for the owner on choices.austinjuliuskim.com. **Preview still dormant** (blank `AdminSubs` → 403). To enable preview: same CLI override with the preview-pool sub (`us-west-2_w74MuQYwu`; prod pool is `us-west-2_JiSiE8xK7`). The CloudWatch dashboard stack (`ops/ops-dashboard.yaml`) was deployed 2026-07-09 with admin creds (stack `ChoicesOpsDashboard`, us-west-2; CI role lacks `cloudwatch:PutDashboard`) — dashboard `ChoicesWebApp-activity`. Still deferred from §10: the Synthetics canary, business/funnel dashboard, frontend Sentry/RUM.

**Data Architecture Stage A LIVE 2026-07-12** (PR #28 merged; preview + prod deployed and smoke-verified same day; `ChoicesEventLakeAlarms` stack deployed, SNS email confirmation pending; ops note: local `sam deploy` as ChoicesUser lacks the new Glue/SQS/etc. perms — use the admin profile locally; CI role is fully updated) — implements [[Choices Data Architecture Plan]] Stage A in full: `backend/events.mjs` (26-type frozen registry: 25 catalog + additive `pairing_deleted`; envelope `{event_id, ts, type, schema_v, pairing_ref, actor_role, payload}`; daily-derived anon salt; `K_ANON_FLOOR`/`applyKFloor`), transactional outbox `EVENT#` items riding the existing `savePairing`/`mutatePairing` transactions, new `track` action (strict per-type validators — free text rejected), `backend/streamConsumer.mjs` → `EventLakeBucket` `raw/`+`anon/` `type=/dt=` JSONL partitions, `backend/compaction.mjs` weekly tombstone rewrite, template.yaml adds Streams/ESM+DLQ/Glue projection tables/Athena workgroup/schedule, frontend beacons across 16 call sites, `docs/event-lake.md`. Small carry-alongs: `billing.mjs` checkout `metadata.plan` (feeds `sub_started`), claimSeat retry via `lostWriteRace()` (transactional claims). Ops in the PR: CI IAM additions (incl. pre-existing preview-table drift fix), verify `AnonSalt` on both stacks, preview smoke, admin-creds deploy of `ops/event-lake-alarms.yaml` (prod-only), no backfill of the old static-salt feed.

**Create-screen Fill-my-4 now personalized 2026-07-14** (branch `worktree-fill4-user-history`, draft PR) — Suggestion Engine "Increment A": `fillForUser` now feeds the signed-in user's own history into the Bedrock prompt via new pure `userHistoryEntries()` in `backend/history.mjs` (folds `USER#.recentGames` into the same `{label, entryCount, winCount, lastAt}` shape as `HIST#`; recentGames only — mixing in all-time `stats.topWinners` would double-count wins). Prompt line generalized "This pair has played before" → "They've played before" to cover both paths. Signed-out create-screen behavior unchanged (sign-in already required). Note: the plan's optional viewer-geo prompt hint was dropped — CloudFront geo headers were rejected in prod (see `handler.mjs` placesSuggest comment); location needs the 📍 consent surface, which Fill-my-4 lacks. Phases 2 (global corpus) and 4 (taste vectors) remain gated per [[Choices Suggestion Engine Plan]].

**Skeleton loaders + premium out-of-turn restart built 2026-07-14** (branch `worktree-choices-skeletons-premium-restart`, draft PR): the last two text loaders replaced with shimmer skeletons extending the `AccountSkeleton` `.sk` system — new `PlayViewSkeleton.jsx` (title/banner/4 choice cards) and `AdminSkeleton.jsx` (stat tiles + panels), same 200ms anti-flash + reduced-motion conventions; button label swaps ("Creating…" etc.) deliberately kept. New **premium perk**: a signed-in premium player may start the next game out of turn (`doRematch` bypasses the `nextStarter` gate when the caller's USER# is premium; game must still be complete; `rematch` in `api.js` now sends the auth header). Free behavior unchanged (alternating starter). Non-premium non-starters see a premium upsell line under the waiting banner (logs `premium-interest`). Both-players-restart race resolves via the existing version lock; the loser's client quietly re-syncs on `GAME_IN_PROGRESS`. Handler tests cover guest/free 409 vs premium 200.

**Brand assets installed 2026-07-14** (branch `choices-brand-assets`, draft PR): trophy-variant logo (dark squircle, 2×2 choice grid, green winner square with trophy glyph — matches the app's indigo/green palette) adopted everywhere. `frontend/public/` gains `favicon.svg` + `icon-32/180` and the stub `icon-192/512` were replaced in place (manifest + `sw.js` push icons pick up the new art with no reference edits); `index.html` gains its first-ever favicon links and a real 180px apple-touch-icon; manifest icon `purpose` corrected `"any maskable"` → `"any"` (transparent squircle corners; a full-bleed maskable variant is a possible follow-up); iOS `AppIcon-512@2x.png` replaced with the 1024 flattened to opaque `#141630` (Apple alpha rule; flattened via a one-off CoreGraphics swift script). In-app: logo now renders beside the Landing h1 (`.brand-row`) and `drawRevealCard` (now async) draws it above the share-card footer, best-effort. Source art archived in `~/Downloads/v2/` (clean + trophy variants, SVG + 32–1024 PNGs).

**CI preview deploys on PR built 2026-07-14** (branch `ci/choices-preview-deploy`, draft PR): new `deploy-preview` job in `choices-webapp.yml` — every same-repo PR touching the app redeploys the shared `ChoicesWebApp-preview` stack (sam deploy + `deploy-frontend.sh`, concurrency-serialized, last-writer-wins) and comments the preview URL on open. **Blocked on two one-time IAM changes** to `choices-webapp-github-deploy` (documented in `docs/feature-deploys.md`, mirrored in `docs/iam-policy.json`): trust policy must also allow the `pull_request` OIDC sub, and the two S3 bucket patterns broaden to `choiceswebapp*sitebucket-*` / `choiceswebapp*suggestdatabucket-*` (everything else already wildcard-matches preview). Also: brand-assets branch was manually deployed to preview the same day (`https://d3r33durbg7s1k.cloudfront.net`, admin profile, sandbox workaround: `npm_config_cache` pointed at a job-local dir).

**Premium UX refinement + store-readiness assessment 2026-07-15** (worktree `choices-premium-ux`, draft PR): five premium-surface changes plus an assessment doc — no palette change.
- **Fill-my-4 is now premium-only** (`AI_FREE_USES` 3 → 0; the LLM call has real per-use cost). The affordance still renders for everyone as a locked teaser (🔒 + "Premium feature"); the server stays authoritative (a real premium account with a cold streak cache still fills on click). Supersedes the "3 free/month" gate above and in [[Choices Suggestion Engine Plan]].
- **Places (Google) is now premium-gated server-side.** `doPlacesSuggest`/`doPlaceDetails` take `user` and check `isPremiumUser()`; guests/free get `{ suggestions: [], premiumRequired: true }` — no billable upstream call. `api.js` now sends the auth header on both. `ChoiceInput.jsx` skips the L3 call for non-premium (L1 pair-memory typeahead stays free for everyone); `NearMeToggle.jsx` shows a locked pin that routes to the upsell.
- **In-app cancel (the cute path):** new `#/cancel` route → `CancelView.jsx` (Choicey mascot `frontend/public/choicey.png` + cheeky plea + Confirm) → new `cancelSubscription` action → `stripe.subscriptions.update(subId, { cancel_at_period_end: true })` (keeps Premium until period end); mirrors `cancelAtPeriodEnd`/`currentPeriodEnd` onto the `USER#` item. The Stripe billing portal stays as a secondary "Manage billing" link.
- **Premium status badge** replaces the buy buttons when subscribed (`PremiumSection` active state → renews/ends date + Cancel/Manage actions); ✨ crest added to the account-corner pill (`premium` threaded through `streakCache.js` from `getMe`, no extra API call).
- **Admin backfill** to unblock a subscription that never linked through the app's own Checkout (e.g. created in the Stripe dashboard, or before the Live webhook was wired): owner-only `adminSetPremium` (ADMIN_SUBS-gated) + `reconcileByEmail()` in `billing.mjs` matches the Stripe customer/sub by email so cancel keeps working; surfaced as a "Premium tools" control in `AdminView.jsx`.
- **Store-readiness assessment** (assessment-only, no UI code changed): `apps/choices-webapp/STORE_READINESS.md` + a shared scorecard artifact — verdict ~80% store-ready (genuinely mobile-first); quick wins = a shared `Button` primitive, ≥44pt touch targets, `:focus-visible` rings, tokenize the literal `#334155` borders; the real paid-launch blocker is StoreKit/Play-Billing IAP (Premium is web-Stripe today, correctly hidden in the shell). The "← Back / Sign out as links" instinct is confirmed (under-44pt tap targets).
- Handler tests updated for 0-free Fill-my-4 + Places premium gating; new tests for `cancelSubscription` and `adminSetPremium` (161 pass). Ops (in the PR): register the Stripe **Live** webhook endpoint + set the Live `STRIPE_WEBHOOK_SECRET`; confirm `ADMIN_SUBS` holds the owner sub.
- **Follow-up same session — preview Stripe Test Sandbox wiring + billing error hardening** (same PR #46 branch): canceling on preview returned a raw **500** because `cancelSubscription` called Stripe with no try/catch and the handler maps only known error classes. Fix: `billing.mjs` now maps Stripe SDK throws to clean `BillingError`s (`resource_missing → SUB_STALE 409`, auth → `STRIPE_AUTH 502`, connection → `STRIPE_UNAVAILABLE 502`) across checkout/portal/cancel/`reconcileByEmail`; `doCancelSubscription` auto-reconciles a missing/stale sub id by email + retries once before a clean `NO_SUBSCRIPTION 400`. The **preview stack was missing `StripeSecretKey`/`StripeWebhookSecret` (sk_test_/whsec_) and `AdminSubs`** — the CI `deploy-preview` job now injects all three from GitHub secrets/vars via `--parameter-overrides` (which *replaces* the samconfig list, so the non-secret preview params are repeated inline — keep in sync). New runbook `docs/stripe-preview.md` (register test webhook endpoint at `…/api/stripe-webhook`, add the 3 GitHub secrets/vars, find the preview-pool Cognito sub, test card 4242). 163 tests pass. Ops: add `PREVIEW_STRIPE_SECRET_KEY`/`PREVIEW_STRIPE_WEBHOOK_SECRET` secrets + `PREVIEW_ADMIN_SUBS` var; register the Stripe **test-mode** webhook for the preview URL.
- **Actual cancel-500 root cause found + fixed (2026-07-15):** after the secrets were wired, cancel *still* 500'd. It was **not** the Stripe error path — the cancel *succeeded* at Stripe but persisting the result crashed on `currentPeriodEnd: undefined`. Newer Stripe API versions moved `current_period_end` off the subscription object onto its **items**, so `subscriptions.update()` returns no top-level field → `updateUserPremium` wrote `undefined` → the `DynamoDBDocumentClient` (no `removeUndefinedValues`) threw a marshalling `Error` (not a `BillingError`) → the unmapped 500. Unit tests missed it because the ddb mock doesn't marshal. Fix: `marshallOptions.removeUndefinedValues: true` on the client (`handler.mjs:55`, also protects the webhook write path) + `subPeriodEnd()` in `billing.mjs` reads the period end from the sub or its first item (used by cancel/reconcile/webhook). Deployed to preview (Lambda LastModified 20:52). Lesson: gate DDB writes against `undefined`, and Stripe `current_period_end` now lives on subscription items.

**App Store / Play Store readiness UI pass (P0 + P1) built 2026-07-15** (branch `worktree-choices-store-readiness-ui`, draft PR): implements the `apps/choices-webapp/STORE_READINESS.md` P0 + P1 checklist — frontend-only, no palette change, no backend touch, Vite build clean (74 modules). **P0:** new shared `frontend/src/Button.jsx` + `NavButton.jsx` primitives (variants default/primary/ghost/danger/link; `busy` prop; `<button>` for actions, `<a>` for nav — identical styling) replace ~30 hand-authored `.btn`/`.link-btn` call sites across 8 view files (only the bespoke `.btn.platform` affiliate anchor left raw); `styles.css` gains `min-height:44px` on `.btn`/`.chip`/`.link-btn`/`.choice-btn`/`.tip-btn`/`.suggestion`, one consolidated `.btn:disabled`, a single global `:focus-visible` outline ring, and the 6 literal `#334155` borders tokenized to `var(--border)`. **P1:** new `TopBar.jsx` (fixed native-style top bar) replaces the floating `.corner-tools` — contextual back on the left via a pure `backTarget(hash)` (no identity coupling: `#/` already resolves to PlayView mid-game or Landing, so back returns to the game when mid-game), the 📍 near-me pin + account pill moved into it; per-view `← Back`/`.admin-back` anchors and the `body.account-corner-active` padding hack removed; a single `@media (min-width:768px)` frames the column as a floating panel for iPad (phone layout untouched). **Correction to `STORE_READINESS.md`:** its scorecard implied the PR-#46 premium components already met the "P0 standard" (44pt targets, focus rings) — they did **not** (`.btn` had no `min-height`; the stylesheet had **zero** `:focus-visible`), so both were net-new here. **Hardware-back deferred** (explicit decision): iOS WKWebView never fires Capacitor's `backButton`, and the planned Android path is a **TWA (Chrome)** where hash-anchor history already gives hardware-back for free — installing `@capacitor/app` would be a no-op on both shells; `backTarget()` is exported so a guarded hook is a trivial future add if a Capacitor Android target ever appears (iOS edge-swipe also deferred — native change, risks stale history). Ships Choices as store-ready for a **free-tier** launch; **P2/IAP (StoreKit / Play Billing) remains the only paid-launch blocker**. Ops: **None** (no new dependency, no env/infra change; CI preview-deploy redeploys on the PR).

**UI/UX bug-fix batch built 2026-07-16** (worktree branch `worktree-choices-ui-fixes`, draft PR): frontend-only polish, Vite build clean (78 modules), no backend/env change. (1) **Logo** — the Landing `.brand-row` is retired; the logo + "Choices" wordmark now live in the persistent `TopBar.jsx` (`.topbar-brand`, centered, `pointer-events:none`) on every view, using the higher-quality `public/icon-512.png` raster instead of the flat `favicon.svg` — supersedes the 2026-07-14 "logo renders beside the Landing h1 (`.brand-row`)" note above (`favicon.svg` stays the browser-tab icon; `drawRevealCard` still draws the logo on the share card). (2) **Tips trimmed** — `TipJar` removed from Landing, Settings, and Premium (felt money-hungry); it still renders (env-gated) on the "Game created" (`CreatePairingView`) and winner-reveal (`PlayView`) screens, so `support.jsx` + tip CSS/`VITE_TIP_*` stay. (3) **History lists** — the stacked "Your winners"/"Recent games" sections in `HistoryView.jsx` are replaced by a segmented pill switcher (`.segmented`/`.seg-tab`) showing one list at a time; `AccountSkeleton` gains a matching pill placeholder. (4) **Bottom nav** — `.bottom-nav` changed from a floating centered pill to a full-width bar pinned flush to the viewport bottom (`left/right:0; bottom:0`, safe-area via `padding-bottom`, top border + upward shadow). (5) **Reveal screen** — the next-game `.rematch` form is no longer a below-card scroll section; it's gated behind the Winner Card via the existing 3D flip: a new "🔄 Start a new game" button (beside "📸 Share the reveal", shown when `iCanRematch`) sets `rematchRevealed`, flipping the card back to the **front face**, which now hosts the rematch form (carded to match `.get-winner`); the `flip-settled` front-hide is gated on `!rematchRevealed` and the reduced-motion cross-fade fallback is unchanged. **Follow-up refinements (same session, same PR #49):** the top-bar brand lockup is now **left-aligned** (`.topbar-brand` a static flex child in `.topbar-left` after the back arrow; 📍 pin stays right) rather than absolute-centered; the bottom nav was **reverted to the floating pill** (the full-width flush bar was dropped — back to centered, rounded, `bottom: safe-area + 12px`); and Recent games gained **client-side pagination, 10/page**, via a prev/next pill pager (`.pager`/`.pager-btn`/`.pager-status`, matching the segmented switcher) — `getMe` already returns the full hard-capped set (≤10 free / ≤50 premium; `RECENT_GAMES_CAP=50`) so it's pure client slicing with a clamped `safePage`, no backend/cursor; Winners stays top-5. Also: the Premium tab's text "Loading…" state was replaced with a new **`PremiumSkeleton.jsx`** (paywall-shaped shimmer — pitch lines + two plan cards + CTA) extending the shared `.sk` system, matching `AccountSkeleton`/`PlayViewSkeleton`/`AdminSkeleton`. Ops: **None** (frontend-only; CI preview-deploy redeploys on the PR).

**Winner-screen new-game + nav-clearance fix built 2026-07-16** (worktree branch `worktree-choices-winner-fixes`, draft PR; follows up the now-merged **PR #49** above, `main`@`aefb76c`): frontend-only, Vite build clean (79 modules), no backend/env change. **Supersedes item (5) of the batch above** — the reveal-screen card *flip* to the next-game form is removed. Tapping "🔄 Start a new game" (still on the winner card's `.reveal-actions`, gated on `iCanRematch`) sets `rematchRevealed`, which now drives a **dedicated full-view new-game screen** via an early return in `PlayView.jsx` (own `<h1>New game 🎲</h1>` + muted subtitle + `FillMyFour` + 4 `ChoiceInput`s + "🎲 Start new game", plus a `.footer` "← Back to results" ghost button) — mirrors `CreatePairingView`'s layout, so there's no leftover "Dinner's decided 🏆" header and no card-in-card. The flip-card front face reverts to the board only; `showBack` simplifies to `complete` and the class to `settled ? "flip-settled"` (the `!rematchRevealed` guards are gone); the now-unused `.rematch` CSS block is deleted (`.reveal-actions` and all `.flip-*` retained). **Bottom-nav clearance (winner-page fix):** `.container` bottom padding raised `safe-area+96px → safe-area+116px` (and the ≥768px branch's bottom margin `+92px → +112px`) so the footer ("You are Player {role} / Leave") always clears the fixed floating pill instead of colliding with it — the `.bottom-nav` pill was already `position: fixed` and a root sibling in `main.jsx`, so it never actually relocated between tabs; the perceived "pushed lower" on the winner page was pure content overflow under the pill. Ops: **None** (frontend-only; CI preview-deploy redeploys on the PR).

**Final Cut brand palette + gold winner + share cut-card built 2026-07-20** (worktree branch `worktree-choices-final-cut-ui`, draft PR): implements the webapp items of [[Choices Brand Book]] "Final Cut" — **gold approved** (household sign-off). (1) **Palette retoken** in `styles.css` `:root`: `--bg` → Table Slate `#0b1020`, `--indigo` → Electric Indigo `#5b5bf7` (`--indigo-dark` → `#4d4dd2`, derived, token still unused), `--red` → Cut Red `#ff4d5e`, `--text` → Menu Paper `#f4f4f0`, new `--gold: #ffc53d`; the six hard-coded indigo tint literals now derive from the token via `color-mix(in srgb, var(--indigo) N%, transparent)` (confetti likewise reads `--indigo`/`--gold` from computed style at fire time); theme-color `#0b1020` in `index.html`, `manifest.json` (both fields), and `handler.mjs` ogPreviewPage (only backend touch). (2) **Gold winner moment**: `.choice.winner .choice-btn` + `.tag.win` → gold; confetti → `["#5b5bf7","#ffc53d"]`; `--green` retained as functional success only (`.banner.your-turn` stays green). (3) **Share cut-card**: `revealCard.js` redesigned to the brand book's signature asset — flat `#0b1020`, Wordle-style 4-row result grid (3 struck rows in Cut Red, gold-stroked `🏆 {winner}` row (trophy kept over the brand book's crown — matches the existing trophy logo)), verdict line "{winner} survived · nobody's fault", locked tagline kept verbatim; same `drawRevealCard(canvas, {winner, losers})` signature so `onShareReveal` plumbing untouched; "Dinner's served." footer line dropped (not locked copy). Vite build clean; 164 backend tests pass. Ops: `npm run build:ios` to refresh the iOS shell copy; `og-card.png` (static 1200×630) still old palette — regeneration is a design follow-up; app-icon rebuild remains the brand book's "Next" item. **Follow-up same session (same PR #52):** trophy kept over crown on the cut card (matches logo); the six indigo tint literals + confetti now derive from theme tokens (`color-mix` / computed-style reads); **persistent game-code line** on the game screen once both players joined — host gets a tap-to-copy pill (`.game-code-line`, shared `copyCode` with the pre-join pinned block), guest gets plain text, shown on active and winner screens so the code is never lost; **mascot renamed Choicey → Cutty** (`cutty.png`, alt/sign/comments, CSS `.choicey` → `.cutty`) and the sticker's flat-gray background made alpha-transparent (edge flood-fill + stray-speck removal via a one-off Pillow script; card frame radius/shadow dropped from the CSS since the art is now a cutout). **Gold reversed 2026-07-20 (same PR):** after seeing it in-app the household reversed the gold call — the winner/celebration accent is back to the previous green `#22c55e`, kept as a new semantic `--winner` token (separate slot from functional `--green`; `--gold` removed); winner tile/tag tints now `color-mix` off `--winner`, confetti reads `--winner`, and the cut-card winner row/verdict are green again (green trophy also matches the logo art). Everything else from the Final Cut pass stands.

**Frontend src/ reorganized into feature folders + PlayView split 2026-07-20** (same PR #52): 34 `git mv` moves into `features/{game,account,premium,admin,places}`/`components`/`hooks`/`lib` with a new `@/` alias (vite.config.js + jsconfig.json) and every intra-src import rewritten to `@/…` (invariant: zero `"./"` imports; dist bundle hash-identical after the move). PlayView.jsx (653 lines) split into co-located `WinnerFace.jsx` (winner card back face; `WinnerAccountLine` moved in from HistoryView — its only consumer, killing the one cross-feature view import), `NewGameScreen.jsx` (stateless next-game screen; choices/fill4-provenance state stays in PlayView so back-and-forth keeps typed input), and `GameCode.jsx` (`PinnedInvite` + `GameCodeLine` with a private `useCopyCode`). No styles.css/backend/index.html changes; barrels deliberately skipped. Verified: build clean, and winner/mid-game-host/mid-game-guest/pre-join states rendered headless against the preview API via a temporary localStorage-bootstrap page (real games driven over curl).

**Input clearing + pinned game code built 2026-07-19** (worktree branch `worktree-choices-input-clear-pinned-code`, draft PR): two iOS-launch UX fixes, frontend-only, Vite build clean. (1) **Choice-input clearing** — `ChoiceInput.jsx` gains a UITextField-`clearButtonMode`-style ⓧ (shown only when the field has text; `onMouseDown` preventDefault keeps the keyboard up; full-input-height 44px hit area via new `.choice-clear`; `.choice-input` right padding 16→44px), so every usage (create form, rematch form, admin) gets per-field clear; the create and rematch forms additionally get an always-rendered ghost **"Clear all"** beside the primary submit in a bottom `.form-actions` flex row (ghost content-sized, primary `flex:1`; disabled/faded when all 4 are empty — placement iterated twice from a conditional link row, then a "YOUR 4" header row, after feedback on tap size and layout shift) that empties all four and resets the `fill4` provenance ref; the per-input ✕ wears the iOS-system treatment (`.choice-clear-glyph`: 18px solid `--muted` disc, dark knocked-out ✕). (2) **Pinned game code + re-share** — the create screen's `joinLink`/share fan-out moved to a new shared `frontend/src/invite.js` (`joinLink(code)` + `shareInvite(code)`; native Share → Web Share → clipboard, copy unchanged); `PlayView`'s static "Share code X" waiting banner (shown while `!bothJoined`, code from stored identity) is now a **pinned invite block**: tap-to-copy code display (`.code-display.pinned`, "Tap to copy"/"Copied!" hint) + a "📤 Share invite" button, so the host can keep sharing after taking their seat if the invite got lost in transit. Ops: **None** (frontend-only; CI preview-deploy redeploys on the PR).

**Persistent account nav built 2026-07-05** (branch `feature/account-corner-nav`, PR open): floating top-right corner pill on all views → `#/account` — subdued "Sign in" chip for guests, 📜 glyph + cached 🔥streak for signed-in (premium, streak ≥ 1). Streak comes from a per-sub localStorage cache written through `getMe` (`frontend/src/streakCache.js`) — zero new API calls, consistent with Tier-1 cost hardening. Backed by market research (Wordle/NYT, Duolingo, Monkeytype, lichess): persistent identity affordance + existing post-game conversion line = "pattern C". Hidden in the iOS shell via `authEnabled`.

## Growth Plan
See **[[Choices Growth Plan]]** — the strategy, monetization, and sequencing doc. The earlier free-growth-experiment framing (no monetization, no accounts, no native apps) was superseded 2026-07-01: Choices is now a real side-business attempt — target $5k+/mo, food-focused ("what should we eat" category), accounts acceptable for premium, Capacitor iOS app on the ladder.

### Carried-over engineering items (not covered in the growth plan)
- Lightweight analytics: event counters for created / joined / completed / rematch (DynamoDB counters or Plausible/PostHog free tier).
- ~~GitHub Actions CI running the backend test suite; add integration tests for `handler.mjs`~~ — done (audit 2026-07-21: `backend-tests` job gates deploys; handler.test.mjs + 8 more suites exist). See [[Choices Harness Audit 2026-07-21]].
- ~~Fix doc drift: README and `docs/PLAN.md`~~ — done (audit 2026-07-21: README describes pairing model; PLAN.md carries a historical banner).
- ~~Variable choice count 3–8 (remove the `EXACTLY_FOUR` constraint in `game.mjs`)~~ — done 2026-07-22 (PR projects#53: 3–8 end-to-end, starter-never-cuts-last turn order, event bounds schema_v 2). Group-mode prerequisite cleared.
- ~~Pairing history + running tally/streak ("A 3 – 2 B")~~ — history shipped (HIST# items + accounts, 2026-07-05); **tally killed 2026-07-21 by Austin** — the game has no per-player winner (`stats.mjs` design note), a head-to-head score conflicts with Constitution rule 6.

## Links
- Part of: [[Projects MOC]]
- Growth strategy: [[Choices Growth Plan]]
- Repo: `personal/projects/apps/choices-webapp`
